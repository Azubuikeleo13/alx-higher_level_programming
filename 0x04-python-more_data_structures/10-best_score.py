#!/usr/bin/python3

def best_score(a_dictionary):

    if a_dictionary == None:
        return None

    for i in a_dictionary.keys():
        if a_dictionary.get(i) > bst:
            nam = i

    return nam
